// peer_tab.dart
import 'package:flutter/material.dart';

class PeerTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Peer Tab'),
      ),
    );
  }
}