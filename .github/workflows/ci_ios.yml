on: [push, pull_request]
# this from  from https://github.com/sunshine-protocol/sunscreen Appache 2.0 license
#

# name is (urlencode) connected to README.md svg badge (change it there as well)
name: iOS Build

jobs:
  ci:
    name: Compile and Build - iOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v1
      # #################### build conditions
      - name: vtag variable
        shell: bash
        run: echo "vtag=$(echo ${GITHUB_REF#refs/tags/} | awk '/v([0-9]+\.)+[0-9]+/{print $0}')" >> $GITHUB_ENV

      - name: Manual extract tag if tag is '*.*.*'
        id: extract_tag
        shell: bash
        run: echo ::set-output name=tag::$vtag

      - name: check tagged
        shell: bash
        run: echo ${{ steps.extract_tag.output.tag }}

      - name: env check tag
        shell: bash
        run: echo ::set-env name=TAGGED2::$(echo ${GITHUB_REF#refs/tags/})

      - name: env check heads
        shell: bash
        run: echo ::set-env name=TAGGED3::$(echo ${GITHUB_REF#refs/tags/} | awk '/([0-9]+\.)+[0-9]+/{print $0}')

      - name: show check
        run: echo ${env:TAGGED}

      - name: show check tag 2
        run: echo ${env:TAGGED2}

      - name: show check tag 3
        run: echo ${env:TAGGED3}

      - name: check alone
        shell: bash
        run: echo ${GITHUB_REF#refs/tags/}

      - name: check alone2
        shell: bash
        run: echo ${GITHUB_REF#refs/heads/}

      - name: check raw
        shell: bash
        run: echo ${GITHUB_REF#refs/tags/} | awk '/([0-9]+\.)+[0-9]+/{print $0}'

      - name: check raw2
        shell: bash
        run: echo ${GITHUB_REF:11} | awk '/([0-9]+\.)+[0-9]+/{print $0}'

      - name: check some
        shell: bash
        run: echo $(${GITHUB_REF:11} | awk '/([0-9]+\.)+[0-9]+/{print $0}')

      - name: check some 2
        shell: bash
        run: echo $(${GITHUB_REF#refs/heads/} | awk '/([0-9]+\.)+[0-9]+/{print $0}')

      # ##################### setup build environment
      - name: Cache LLVM and Clang
        id: cache-llvm
        uses: actions/cache@v2
        with:
          path: ${{ runner.temp }}/llvm/10.0
          key: ${{ runner.os }}-cached-llvm-10.0

      - name: Install LLVM and Clang
        uses: KyleMayes/install-llvm-action@v1
        with:
          version: "10.0"
          directory: ${{ runner.temp }}/llvm/10.0
          cached: ${{ steps.cache-llvm.outputs.cache-hit }}

      - name: Checkout sources
        uses: actions/checkout@v2

      - name: Install Rust Toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly
          override: true
          components: rustfmt

      - name: Install WASM Target
        run: rustup target add wasm32-unknown-unknown

      - name: Cache Cargo
        uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            target/aarch64-apple-ios
            target/x86_64-apple-ios
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Cargo Tools (cargo-make)
        uses: actions-rs/cargo@v1
        continue-on-error: true
        with:
          command: install
          args: cargo-make

      - name: Install Cargo Tools (cbindgen, v0.14.4)
        uses: actions-rs/cargo@v1
        continue-on-error: true
        with:
          command: install
          args: cbindgen --version 0.14.4

      - name: Install Cargo Tools (dart-bindgen)
        uses: actions-rs/cargo@v1
        continue-on-error: true
        with:
          command: install
          args: dart-bindgen --features cli

      - name: Setup Flutter
        uses: subosito/flutter-action@v1
        with:
          channel: "stable"

      - name: Run Flutter pub get
        run: flutter pub get

      - name: Install iOS Targets
        run: rustup target add aarch64-apple-ios x86_64-apple-ios

      # ############### Release/Debug build dependent
      - name: Run cargo make ios  - Release
        if: steps.extract_tag.output.tag != ''
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: make
          args: ios --profile release

      - name: Run cargo make ios  - debug
        if: steps.extract_tag.output.tag == ''
        uses: actions-rs/cargo@v1
        continue-on-error: false
        with:
          command: make
          args: ios --profile debug

      - name: Build iOS App - release
        if: steps.extract_tag.output.tag != ''
        run: flutter build ios --release --no-codesign --simulator

      - name: Build iOS App - debug
        if: steps.extract_tag.output.tag == ''
        run: flutter build ios --debug --no-codesign --simulator


      # ####################### upload intermediate result dependent if release in text or tagged
      - name: Upload Release - Release with tag
        id: upload-release-tagged-asset
        if: steps.extract_tag.output.tag != ''
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./build/ios/iphoneos/Runner.app
          asset_name: adbfflutter-ios-intermediate-${{ steps.extract_tag.output.tag }}.app
          asset_content_type: application/octet-stream
